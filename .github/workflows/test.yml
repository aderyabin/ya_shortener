name: go test

on:
  pull_request:
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    container: golang:1.26

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run tests
        run: go test ./... -v -cover
